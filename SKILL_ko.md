---
name: gpt-build-harness
description: >-
  ChatGPT 구독 쿼터로 실행하는 GPT 잡부 둘의 운용 지침이다. Daybreak Blue
  (gpt-daybreak-blue)는 백엔드를 맡고, ASTRA(gpt-6-astra)는 백엔드와
  프론트엔드 연결, 여러 분야의 지식이 필요한 구현, i18n 카탈로그와 감사를 맡는다.
  화면은 GPT에게 맡기지 않는다. Claude Opus 5.5가 그리며, GPT의 쿼터를 다 쓰면
  두 워커의 일도 Opus 5.5가 대신한다. 실측한 모델 정보(슬러그, effort 값,
  컨텍스트 윈도우), ClaudeRipple 라우터를 통해 어느 세션에서나 Agent() 한 번으로
  두 워커를 부르는 방법, 작업서 작성법, 쿼터 소진 시 대처법과 주의사항을 담았다.
  세션에서 gpt-worker나 astra-worker에게 처음 위임하기 전에 읽는다.
  호출을 라우팅하지 못하거나, 워커의 결과물을 Claude가 쓴 듯하거나,
  워커가 보고하지 않은 이유로 검증이 실패할 때도 읽는다.
  누구에게 어떤 일을 맡길지는 이 문서에 없다.
  그 방침은 ~/.claude/rules/gpt-workers.md에서 항상 불러온다.
---

> `SKILL.md`의 한국어 역본이다. 모델이 읽을 때 토큰이 훨씬 덜 드는 영어를 정본으로 삼으며, 한국어 역본은 저장소에만 두고 설치하지 않는다.

# GPT 빌드 하네스 (Daybreak Blue + ASTRA)

GPT를 잡부로 붙여 구현과 검증을 맡긴다. Claude는 아키텍처, 설계 방향, 통합과 최종 판단을 담당한다. 호출에는 ChatGPT **구독 쿼터**를 쓰므로 Claude가 전부 구현할 때보다 Claude 토큰을 아낀다.

**누구에게 어떤 일을 맡길지는 이 문서에서 다루지 않는다.** 그 방침은 세션마다 불러오는 `~/.claude/rules/gpt-workers.md`에 있다. 여기에는 실제로 위임할 때 필요한 실측 모델 정보, 연결 방식, 작업서 작성법과 주의사항을 담았다.

> 2026-08-19 이전 문서에서 'GPT 사용법'에 해당하던 내용(luna/sol/terra 등급표, effort 실측표, 프롬프트 A/B 시험 묶음)은 사실을 지어낸 대목이 너무 많아 전부 폐기했다.

## 실측으로 확인한 모델 정보

**Daybreak Blue는 백엔드를 맡고, ASTRA는 연결 작업, 자료 확인과 감사를 맡는다.** 둘 다 화면은 그리지 않는다. 2026-09-25부터 화면은 Claude Opus 5.5의 일이다. GPT 모델은 이 둘만 부른다. luna/sol/terra를 비롯한 나머지 5.x 계열은 호출 대상에서 제외했다.

- **Daybreak Blue의 슬러그는 `gpt-daybreak-blue`이며 버전 접두사가 없다.** 2026-08-19에 codex responses 백엔드로 확인했다. `gpt-5.6-daybreak-blue`와 `gpt-5.6-daybreak`는 HTTP 400을, `gpt-daybreak-blue`만 200을 반환한다. GPT-5.6 Sol의 튜닝판이며, 백엔드 작업에서는 구독 쿼터를 덜 쓰면서도 성능이 같아 계속 기본 모델로 쓴다.
- **ASTRA의 슬러그는 `gpt-6-astra`다.** 2026-09-05에 실제 호출로 확인했다. 200을 반환하고 응답에도 `model: gpt-6-astra`를 적어 보내며, `gpt-6`, `gpt-astra`, `gpt-6-astra-latest`는 모두 HTTP 400을 반환한다.
- **effort는 `high`가 기본이며 가장 쓸 만하다.** `high`, `xhigh`, `max` 셋을 쓰고, `low`와 `medium`은 쓰지 않는다. Daybreak의 `max`는 도저히 풀기 어려운 문제에만 쓴다. ASTRA는 high보다 높은 두 값도 허용하지만 **아직 측정하지 않았다.** 필요성을 판단해서 바꾸고 결과를 보고한다.
- **`ultra`는 보낼 수 없다.** responses 엔드포인트는 두 모델 모두에서 HTTP 400을 반환하며 지원 값으로 none/minimal/low/medium/high/xhigh/max를 나열한다(2026-09-05 실측). Ultra는 Codex 클라이언트의 모드다. Codex 앱과 CLI에서는 ASTRA에 선택할 수 있지만 여기서는 쓰지 못한다. 라우터는 `ultra`를 그대로 보내지 않고 `max`로 바꾼다.
- **모델마다 컨텍스트 윈도우가 다르며, `contextWindow`는 압축을 시작하는 토큰 수가 아니다.** Claude Code는 `contextWindow - min(maxOutputTokens, 20000) - 13000`에서 압축한다. 비율로 계산하지 않고 고정량을 남긴다(2.1.258 바이너리에서 확인). `behavesAs`로 Opus의 출력 상한을 쓰면 남기는 양은 33,000으로 고정된다. 따라서 설정에는 압축을 시작할 토큰 수에 33,000을 더해 적는다. Daybreak는 500k에서 압축하도록 533000, ASTRA는 860k에서 압축하도록 893000을 쓴다. 주의사항 문서를 참고한다.
- **ASTRA는 `version`을 보내지 않을 때가 아니라 오래된 버전을 명시할 때 거부한다.** 호출자가 오래된 CLI 버전을 보내면 백엔드는 `The 'gpt-6-astra' model requires a newer version of Codex`(HTTP 400)로 답한다. 0.144.1은 거부했고 0.153.4는 허용했다(2026-09-05 실측). 라우터는 `version` 헤더를 아예 보내지 않으며, 이 요청은 통과한다(2026-09-18 실측). 오래된 값을 적느니 보내지 않는 편이 안전하다.

## 라우팅

모든 요청을 라우터 하나가 처리하므로 호출 방식도 하나다. `Agent(gpt-worker)`는 Daybreak, `Agent(astra-worker)`는 ASTRA를 부른다. 세션마다 base URL을 지정하는 대신 `settings.json`으로 라우터에 연결하므로 둘 다 어느 세션에서나 작동한다. 한 번의 호출에서만 모델이나 effort를 바꾸려면 작업서 첫 줄에 표식을 넣는다.

```
[[gpt: <model>@<effort>]]
```

설치 방법, 설정 파일, 라우팅 실패 시 대처법은 `references/routing.md`에 있다.

Opus 5.5는 별도의 에이전트 정의가 필요 없다. Agent 도구의 `model` 인자에 `"opus"`를 쓰면 최신 Opus를 따른다. 2026-09-25 실측에서는 `claude-opus-5-5`로 요청을 보냈다. 메인 세션이 직접 그리지 않을 화면은 `Agent(subagent_type: "general-purpose", model: "opus")`로 맡긴다. 이 인자는 에이전트 파일의 `model:`보다 우선하므로, 아래 쿼터 소진 시 대체 호출에도 쓴다.

## 쿼터를 다 썼을 때

GPT 모델 둘은 같은 ChatGPT 구독 쿼터를 쓴다. 어느 쪽이든 429 `usage_limit_reached`가 오면 둘 다 쿼터를 다 쓴 것이다. 2026-09-18에 같은 인증 정보로 두 슬러그를 1분 안에 호출하자 둘 다 429를 반환했다. Agent 결과와 `clauderipple logs`에서 이 실패를 확인할 수 있다.

쿼터가 회복되기를 기다리지 않는다. 같은 워커와 같은 작업서로 다시 호출하되 `model: "opus"`를 추가한다. 워커의 지침은 유지하고 모델만 바꾼다. 2026-09-25 실측에서 이렇게 호출한 `gpt-worker`는 `claude-opus-5-5`로 응답했다. 그 전에 작업서 첫 줄의 모델 표식을 제거한다. 라우터는 어떤 모델에든 표식을 적용하므로, 표식이 남아 있으면 대체 호출도 쿼터가 소진된 GPT로 보낸다.

## 역할을 이렇게 나눈 이유

역할 구분은 위임 방침에 명시되어 있다. 누구에게 맡길지 애매할 때는 그 이유를 알아두면 도움이 된다.

**Daybreak는 백엔드 구현과 감사를 아주 잘하지만 전체 맥락을 고려하는 데 어려움이 있다.** 구현, 검토, 검증과 지식 조회에는 아낌없이 쓴다. 전체 구조를 알아야 올바르게 결정할 수 있는 일은 맡기지 않는다. 모듈 경계, 인터페이스, 데이터 흐름, 오류 정책과 CI/CD 설계가 여기에 해당한다. Daybreak에게 구조 설계를 맡기면 개별 코드만 볼 때는 그럴듯해도 전체와 어긋나는 결과가 나온다.

**Daybreak에게 프론트엔드를 절대 맡기지 않는다.** 미감이 너무 나빠 사람이 쓸 수 없는 결과를 낸다. 예외는 없다.

**ASTRA는 이제 화면을 그리지 않는다.** 2026-09-05부터 2026-09-25까지는 이미 구현한 프론트엔드 안에서 화면을 그렸다. Opus 5.5가 더 잘 그리는 데다 프론트엔드를 처음부터 만들 수도 있어, 화면은 Claude에게 맡기기로 했다. ASTRA는 여러 분야의 지식이 필요한 일을 계속 맡는다. 백엔드가 제공하는 기능에 프론트엔드의 호출, 상태와 이벤트 처리를 연결하는 일, 일반적인 백엔드 지식만으로 풀기 어려운 구현(플랫폼 API, 프로토콜, 외부 명세, Daybreak가 풀지 못하는 문제), i18n 카탈로그와 감사다. 카탈로그는 자연스러운 문장이 관건이며, ASTRA가 Claude보다 감상적이거나 꾸민 표현을 잘 피한다는 사용자의 판단에 따라 계속 ASTRA에게 맡긴다(2026-09-25). 연결 작업 중 화면 파일을 열어도 호출과 상태만 바꾼다. 마크업과 스타일은 바꾸지 않으며, 화면의 목적도 정하지 않는다.

## 백엔드 전체를 위임하는 방식

가장 많이 절약하는 운용 방식이다.

1. Claude가 아키텍처와 인터페이스를 확정한 뒤 백엔드 전체를, 또는 모듈별로 Daybreak에게 위임한다.
2. Daybreak가 구현하고 코드와 함께 **프론트엔드에 연결할 목록**(엔드포인트, 함수 시그니처, 이벤트, 설정 키)을 돌려준다. 작업서에서 이 목록을 명시적으로 요구한다.
3. Claude가 목록을 받아, 사소한 수정을 넘는 연결 작업은 ASTRA에게 맡긴다. 화면 자체는 Opus 5.5가 그린다. 메인 세션이 Opus 5.5면 직접 그리고, 다른 모델이거나 한 번에 맡길 양이 많으면 `model: "opus"`로 Agent를 호출한다. Claude가 통합 빌드와 테스트를 실행해 결과를 판단한다.

## 작업서 작성법

- **작업서만으로 일을 알 수 있게 쓴다.** 워커는 도구로 저장소를 읽을 수 있지만 이 대화는 모르는 채 시작한다. 설계와 시그니처를 확정하고, 수정해도 되는 파일, 검증 명령과 보고 형식을 적는다. 경로는 모두 절대경로로 쓴다.
- **완료 조건을 명시한다.** 예를 들어 단일 파일로 컴파일해야 한다거나, 연결 목록을 별도 절로 보고해야 한다고 적는다.
- **검증 명령은 포그라운드에서 실행하게 한다.** 빌드와 테스트 명령을 포그라운드로 실행하고, 결과를 보고한 뒤에만 턴을 끝내라고 지시한다.
- **문서를 확인하게 한다.** 확신이 없는 API는 기억으로 쓰지 말고 확인하라고 지시한다. 정확한 문구가 필요하면 URL을 적어 주고 WebFetch를 쓰게 한다. 워커의 WebSearch는 소형 Anthropic 모델에 별도 요청을 보내며, 제목과 URL만 돌려준다.
- **Opus 5.5 서브에이전트에게 화면을 맡길 때는 설계 방향도 적는다.** 쓸 토큰과 정의 위치, 본으로 삼을 이웃 컴포넌트의 경로, 상태 목록, 바꾸면 안 되는 인터페이스 id, 검증 명령을 넣는다. 프론트엔드가 아직 없으면 토큰과 이웃 컴포넌트 대신 화면의 목적과 프레임워크를 지정한다.
- **ASTRA에게 연결 작업을 맡길 때는** Daybreak의 연결 목록, 수정해도 되는 호출부와 상태 파일, 수정하면 안 되는 마크업과 스타일 파일을 함께 적는다.
- **적용과 검증은 Claude가 한다.** 워커의 자기 보고만으로 검증을 대신하지 않는다. Claude가 직접 빌드와 테스트를 실행한다.

수정할 파일이 겹치지 않으면 같은 워크트리에서 워커 둘을 동시에 실행한다.

## 주의사항

`references/pitfalls.md`에 정리했다. 특히 자주 겪는 문제는 다음과 같다.

- **워커의 자기 보고와 결과물은 별개다.** 결과물을 직접 연다.
- **Windows에서 셸을 허용할 때는 셸 도구 둘 다 허용한다.** `Bash`만 허용한 워커가 `PowerShell`을 고르면 자동으로 거부된다. 그러면 검증하지 못한 코드를 제출한다.
- **워커는 스스로 서브에이전트와 워크트리를 만든다.** 보고만 믿지 말고 작업 트리를 확인한다. 작업서에는 결과물을 쓸 절대경로를 명시한다.
- **에이전트와 Workflow 결과의 HTML 엔티티는 화면에 표시하는 과정에서 생긴다.** 손으로 복원하지 말고 `journal.jsonl`에서 원문을 읽는다.
- **워커의 WebSearch는 워커 모델이 아닌 Anthropic 모델로 실행한다.** 검색은 작동하지만 ChatGPT 대신 Claude에 과금하며, 제목과 URL만 반환한다.
